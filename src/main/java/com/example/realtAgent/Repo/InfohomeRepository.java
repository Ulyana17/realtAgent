package com.example.realtAgent.Repo;

import com.example.realtAgent.Entity.Infohome;
import org.springframework.data.repository.CrudRepository;

public interface InfohomeRepository extends CrudRepository<Infohome, Long> {
}
